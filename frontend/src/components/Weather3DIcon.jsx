import React from 'react';
import WeatherSymbol from './WeatherSymbol';

/**
 * Weather3DIcon alias: Delegates to the high-fidelity, vibrant WeatherSymbol component.
 */
export default function Weather3DIcon(props) {
  return <WeatherSymbol {...props} />;
}
